
4-一覧ページと詳細ページを共通レイアウト化にする

◆一覧ページの編集◆

ディレクトリ
src/main/resources/templates/tasks/list.html

・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・

<html lang="ja"
      xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layout/layout}"
>

<body>

<section layout:fragment="content">
    <h1>タスク一覧</h1>
    <a th:href="@{/}">トップページに戻る</a>

    <div>
        <table>
            <thead>
            <tr>
                <th>ID</th>
                <th>概要</th>
                <th>ステータス</th>
            </tr>
            </thead>
            <tbody>
            <tr th:each="task : ${taskList}" th:object="${task}">
                <td><a th:href="@{/tasks/{id}(id=*{id})}" th:text="'#' + *{id}"></a></td>
                <td><a th:href="@{/tasks/{id}(id=*{id})}" th:text="*{summary}"></a></td>
                <td th:text="*{status}"></td>
            </tr>
            </tbody>
        </table>
    </div>
</section>

★コード解説★
sectionタグをlayout/layout.htmlの下記に渡している

<section layout:fragment="content"></section>


/////////////////////////////////////////////////////////////////////////////////////////////////

◆詳細ページの編集(遷移元)◆

ディレクトリ
src/main/resources/templates/tasks/detail.html

・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・・

<html lang="ja"
      xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layout/layout}"
>

<body>
<section layout:fragment="content">
    <h1>タスク詳細</h1>
    <a th:href="@{/tasks}">タスク一覧</a>
    <div th:object="${task}">
        <h2 th:text="'#' + *{id} + ' ' + *{summary}"></h2>
        <p th:text="*{status}"></p>
        <pre th:text="*{description}"></pre>
    </div>
</section>

/////////////////////////////////////////////////////////////////////////////////////////////////

